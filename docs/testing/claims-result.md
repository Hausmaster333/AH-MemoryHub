# Структурированные ссылки — 10 сентября 2026

Модель возвращает `claims: [{text, evidence_ids}]`, приложение формирует inline-ссылки. В каждом claim допускается одно предложение; отсутствующие и неизвестные источники, противоречивый отказ и некорректная структура отклоняются. Старый формат ответов продолжает проходить прежний строгий валидатор. Общий builder запроса используется в приложении и обеих ветках пилота. Проверка принадлежности ID не доказывает истинность утверждения — семантическая проверка остаётся отдельной.

Все91тест прошёл; после финальных проверок типа JSON и импорта записей затронутые тесты прошли отдельно. Добавлена проверка усечения генерации. Исправлено сохранение трассы для отказа без working_memory. При переносе ответов между версиями benchmark CLI допускает `--responses <old-run>`: исходные запросы должны совпасть побайтно как JSON-объекты, происхождение фиксируется SHA256. Это не изменение старой записи и не новый запрос к модели.

## Парный локальный пилот

Qwen2.5 7.6B, те же35вопросов и векторные контексты; память AH из93фактов после исправления ложных состояний. Новый формат и prompt одинаковы для обеих веток. Из70ответов64перенесены из прерванного run при точном совпадении запросов,6завершены локально. Полный offline replay проверен.

AH:27содержательных ответов и8отказов, **все35 прошли форматную валидацию**. В прежнем пилоте проходили только2содержательных ответа. RAG:21содержательный ответ и13отказов прошли валидацию;1ответ отклонён за несколько предложений в одном claim. Невалидные ID не разрешались и inline-валидатор не ослаблялся.

Post-hoc просмотр содержания: AH27ответов по существу верны,5правильных отказов,3пропуска. Отдельно отмечена неправильная форма фамилии «Беловым» вместо формы женской фамилии «Белова»; это дефект атрибуции, а не безупречное качество генерации. RAG17ответов верны,3правильных отказа,10пропусков,5неверных ответов: подмена объекта, перенос чужих инструментов, неподтверждённые добавления. Подробности в `results/qwen-claims-review.json`.

В пилоте теперь сохранён дамп графа и соответствие evidence_id↔UID; проверены принадлежность каждого факта working_memory/answer_path и совпадение цитат с доказательствами сохранённого узла. Официальный ExplainScore и M5 не рассчитаны: нет независимого арбитра, коммерческой парной ветки и большого принятого корпуса. Нельзя объявлять подтверждение Senior по этим35знакомым вопросам.

Замороженный код пилота находится в workspace `outputs/ah-review/citation-fix`; применяемый финальный код — `citation-final`, включает дополнительную проверку JSON-объекта. Повторный run новым кодом должен иметь новый out; старый raw не перезаписывать.

## Корпус

Подготовлены30различных сценариев симулированных журналов оператора и локальный генератор черновиков. Первые3черновика после расширения —1367слов. Обнаружены внутренние ошибки (например, направление ошибки температурного датчика) и неподтверждённые нормативные утверждения; **они не приняты как gold или Senior-корпус**. Следующий шаг — редакторская проверка источников и отдельная фиксация вопросов. Генератор не отправляет тексты во внешние API.
