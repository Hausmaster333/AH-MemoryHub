# Блок QA, 8 сентября 2026

В проект применены исправления временных состояний, отрицаний причинности/инструментов, соответствия вопроса событию и идентификаторам, выбора нескольких причин и обхода первопричины. Повторные свидетельства одной причины не считаются разными причинами; неоднозначная первопричина и циклы дают отказ. Даты и произвольные отрицательные факты не получили полноценной семантической модели.

Добавлены backend/scripts/evaluate_qa.py, docs/testing/qa-regression.json и проверка корпуса в pytest. Полный путь через API: текст → автоматическое наполнение → вопрос → ответ/отказ → цитаты и зависимость от seed. 30 регрессионных случаев: до блока 15/30, после 30/30. Этот набор использован при исправлениях, не является независимой выборкой и не закрывает официальный M2. Полные ответы/трассы в outputs/ah-review/qa-final-30.json; компактные отчёты сохраняются в проекте.

M1 с прежним morph_v1, gold и теми же 10 сырыми ответами: required_roles_weighted_f1 3.5417866206043547 → 3.5477342961172478 / 5. Все роли weighted_f1 5.570683649501384 → 5.588511129346911 / 8. Повторное восстановление временной границы больше не создаёт лишний HAS_STATE, когда модельный факт уже покрывает это состояние с TIME. Общая точность извлечения всё ещё ограничена. Новых платных вызовов не было.

81 тест прошёл в реальном проекте, git diff --check без ошибок (только CRLF notices). Дизайн .pen и старый UI-отчёт не менялись в этом блоке.

Backend перезапущен из outputs/ah-review/restart_qa.py. Память и ingestion records сравнены до/после восстановления — точное равенство JSON. Backup before-qa-memory.json и before-qa-ingestions.json. После восстановления проверены запросы; обычный integrated_v1 дал ответ «Остановка агрегата привела к снижению давления.» с trace_complete=true. Та же старая демопамять в directed_v1 дала insufficient_evidence: автоматическая миграция старой памяти не проводилась. Сквозной новый корпус наполнялся текущим компилятором и проверен именно на directed_v1.

Далее: сквозная QA-проверка памяти из реального сохранённого модельного корпуса; расширение разнообразного корпуса для Senior; реальный M4/M5 AH/RAG. Нельзя переносить 30/30 на общую точность движка или заявлять полную готовность Senior.
