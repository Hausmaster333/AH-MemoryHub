# Независимые вопросы и локальная модель — 10 сентября 2026

Новые тексты и 28 вопросов зафиксированы до исправлений (`qa-independent-v1.json`). Базовый результат 17/28, после исправлений 28/28. Это небольшой диагностический набор; после подгонки к найденным ошибкам он стал регрессионным, а не оценкой неизвестных данных.

Исправлены смешение коротких имён в вопросах об инструменте, несовпадающее состояние во временных вопросах, пассивная причина «вызвана не X, а Y», недостоверное принятие условных/предположительных причин, падежи коротких слов вроде «печь/печи» и выбор чрезмерно длинной дублирующей цитаты. Общий извлекатель инструментов теперь используется также rule-провайдером. Отрицательные вопросы пока получают отказ: отрицательные факты не представлены в модели памяти. Проверки идентификаторов и неоднозначной кореференции сохранены.

Все 85 существующих тестов прошли. Дополнительная проверка локального CLI и replay прошла отдельно. Окончательный offline replay DeepSeek: 181 факт, прежние 35 вопросов — 35/35, M1 morph_v1 = 3.5999082091607257/5. Сырые ответы не изменялись, платных вызовов не было. Это не официальный M2 и не завершённое сравнение M4/M5.

`evaluate_corpus.py --provider local --model qwen2.5:7b-instruct-q5_K_M --matcher morph_v1 --out <report.json>` использует установленный Ollama на 127.0.0.1:11434. Ответы и предсказания сохраняются по документам; `--replay <report.records>` повторяет обработку без сети, сохраняя идентичность модели. Для этого прогона Ollama запущен с контекстом 8192, модель 7.6B, Q5_K_M. Локальный прогон ещё выполняется; результаты будут записаны отдельно.

Далее: оценить полноту извлечения Qwen и QA на общей памяти, подготовить разнообразный корпус >=15000 слов, затем реальный AH/RAG эксперимент. Синтетический нагрузочный корпус не заменяет содержательную проверку Senior.
